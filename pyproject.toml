[project]
name = "paisa"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pymongo>=4",
    "pydantic>=2",
    "fastapi>=0.115",
    "uvicorn>=0.32",
]

[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"
